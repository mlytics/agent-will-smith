# Core dependencies - works with Python 3.11, 3.12, and 3.13
# For Python 3.13, if you get Rust errors, try:
# pip install --only-binary :all: -r requirements.txt
fastapi
uvicorn[standard]
google-generativeai
httpx
redis
python-dotenv
pydantic
pydantic-settings
beautifulsoup4
lxml
sse-starlette
tenacity
aiofiles

# Testing dependencies (optional)
requests

