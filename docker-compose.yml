services:
  api:
    build: .
    ports:
      - "8888:8888"  # Host:Container - map host 8888 to container 8888
    env_file:
      - .env
    volumes:
      - ./cache:/app/cache
    restart: unless-stopped

